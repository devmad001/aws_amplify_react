mutation INSERT_CAMERA_CHECKLIST($camera_id: Int!, $name: String!, $order: Int!) {
  insert_camera_checklists_one(object: {camera_id: $camera_id, name: $name, order: $order}) {
    id
    camera_id
    name
    order
  }
}
