// @flow

export type Bypass = {
  id: number,
  site_id: number,
  start_time: string,
  end_time: string,
}

export type User = {
  uid: string,
  name: string,
  email: string,
  displayName: string,
}
